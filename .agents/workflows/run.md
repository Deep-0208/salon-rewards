---
description: Start the development server and show running status.
---

Start the Next.js development server.

Rules:
- If a server is already running, notify the user with the localhost URL.
- Otherwise, run `npm run dev` in the background.
- Provide the URL (http://localhost:3000) for testing.
